// Card/list presets for the Personal Vault, matching the rest of the admin panel:
// cream surfaces, soft borders, and the same rounded geometry as the dashboard cards.

export const glassCard =
    "rounded-[24px] border border-[#EEEAE2] bg-white shadow-[0_1px_3px_rgba(0,0,0,0.04)]";

// Interactive list items (links, notes, files, credentials).
export const glassItem =
    "rounded-2xl border border-[#EEEAE2] bg-white transition-colors hover:bg-[#F6F4EF]";
