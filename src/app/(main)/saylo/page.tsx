import SayloIndex from "@/components/pages/SayloIndex";
import { METADATA } from "@/config/seo.config";

export const metadata = METADATA.saylo;

export default function SayloPage() {
    return <SayloIndex />;
}
