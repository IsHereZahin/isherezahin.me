import { ProjectDetailsLoading } from "@/components/ui";

// Shown instantly while an individual project is fetched/rendered.
export default function Loading() {
    return <ProjectDetailsLoading />;
}
